{
  "name": "test",
  "version": "1.0.0",
  "description": "Test suite for stacked-dirs",
  "scripts": {
    "test": "mocha ./test.mjs",
    "setup": "npm install && cd .. && npm install",
    "clean": "rm -rf node_modules"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chai": "^4.2.0",
    "mocha": "^8.1.1",
    "npm-run-all": "^4.1.5"
  }
}
