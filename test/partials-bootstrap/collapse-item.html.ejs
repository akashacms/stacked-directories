<div class="card">
    <div class="card-header row" id="heading-<%- id %>">
      <% if (typeof thumburl !== 'undefined' && thumburl !== '') { %>
      <span class="col-auto">
        <img src="<%= thumburl %>" style="max-width: 100px; width: 100%;"/>
      </span>
      <% } %>
      <h2 class="mb-0 col">
        <button class="btn btn-link<%
                    if (typeof collapsed !== 'undefined' && collapsed !== '') { %> collapsed <% }
                %>" 
                type="button" 
                data-toggle="collapse" 
                data-target="#collapse-<%- id %>" 
                aria-expanded="<%
                    if (typeof collapsed !== 'undefined' && collapsed !== '') { %>false<% }
                    else { %>true<% }
                %>" 
                aria-controls="collapse-<%- id %>">
          <%= title %>
        </button>
      </h2>
    </div>

    <div id="collapse-<%- id %>" 
            class="collapse <%
                if (typeof collapsed === 'undefined' || collapsed === '') { %> show <% }
            %>" 
            aria-labelledby="heading-<%- id %>" 
            data-parent="#<%- parentID %>">
      <div class="card-body">
        <%- content %>
      </div>
    </div>
</div>